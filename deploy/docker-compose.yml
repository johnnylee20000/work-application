version: '3.8'
services:
  work-application:
    # Image published to GHCR for this repository owner
    image: ghcr.io/johnnylee20000/work-application:latest
    container_name: work-application
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/app.db
    ports: []
